/** @format */

import React from "react";
import Input from "./input";

export default {
	title: "Input",
	component: Input,
};

export const primary = () => {
	return <Input placeholder={"add text here"} />;
};
